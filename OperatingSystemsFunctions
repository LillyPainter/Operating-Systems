The OS is like a Messenger. The Interface between the user and the hardware. The OS relays messages from the user through the command line to the correct drive that responds. The OS sends these messages and organizes them. 
